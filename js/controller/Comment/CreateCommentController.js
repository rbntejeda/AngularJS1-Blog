'use strict';
appBlog.controller('CreateCommentController', function($scope, commentResource, $route){
		// $scope.users = $route.current.locals.users;
		// console.log($route.current.locals.users);
});